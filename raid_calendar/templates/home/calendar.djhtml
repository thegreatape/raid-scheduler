{% extends "home/wrap.djhtml" %}
{% block content %}
<h1>{{date}}</h1>
<div class="cal-wrap">
  {% for date in days %}
  <span class="cal-cell">
	{{date.day|default:""}}<br/>
	{% if date.raids %}
	{% for raid in date.raids %}<a href="{{raid.url}}">{{ raid.title }}</a>{% endfor %}<br/>
	{% endif %}
  </span>
  {% endfor %}
{% endblock %}

